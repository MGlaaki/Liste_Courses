<table width=100% class="table-hover table">
  <thead>
    <tr>
      <th>Vos listes</th>
      <th></th>
    </tr>

  <% listes_user_actif(@listes).each do |l|%>
    <tr>
      <td style="width: 50%"><%= link_to "", liste_path(id: l.id), class: 'btn-sm glyphicon glyphicon-trash',method: :delete %>
      <%=link_to l.nom_liste, liste_path(id: l.id)%></td>
      <td>Partagé avec : <%= l.partages.to_a.size > 0 ? l.partages.to_a.map{|p| p.destinataire_pretty_name}.join(", ") : "personne" %></td>
    </tr>
<%end%>
</thead>
</table>
